import Subscriber from '../models/Subscriber.js'; import nodemailer from 'nodemailer';
export async function sendNewsletter({subject='Bharatvaani Prangan Latest News', html='<h1>Latest News</h1>'}={}){ if(!process.env.EMAIL_USER || !process.env.EMAIL_PASS) return {skipped:true, reason:'EMAIL_USER/EMAIL_PASS missing'}; const subs=await Subscriber.find(); const transporter=nodemailer.createTransport({service:'gmail', auth:{user:process.env.EMAIL_USER, pass:process.env.EMAIL_PASS}}); for(const s of subs){ await transporter.sendMail({from:process.env.EMAIL_USER,to:s.email,subject,html}); } return {sent:subs.length};}
